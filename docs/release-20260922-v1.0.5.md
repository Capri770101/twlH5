# H5 v1.0.5 发布记录

## 范围

- DIY 方案卡内的贺卡编辑器和渲染入口。
- 智能体 IP 代理与 IP 访问 H5 的域名停用兼容。
- 不改变商家后端、订单、支付和退款服务。

## 验收入口

```text
http://129.204.85.139/
```

直接使用 IP 的 HTTPS 可能出现证书名称提示；当前推荐 HTTP IP 验收。

## 发布后检查

- H5 首页 HTTP 200。
- `/agent/health` HTTP 200。
- `twlh5-static`、`twlh5-api`、`flower-shop` 均 active。
- AI 顾问进入 DIY 卡后可打开“配一张贺卡”。
- 贺卡草稿生成失败、超时和取消均有可读提示。
- 本次不自动发送贺卡、不自动创建订单。
