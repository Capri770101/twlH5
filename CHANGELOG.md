# Changelog

## [1.0.4] - 2026-09-22

- 增加域名停用期间的 IP 访问路由：`http://129.204.85.139/`。
- H5 服务端智能体代理默认目标切换为 `https://49.232.49.176`，仍由服务端注入平台 Key，浏览器不持有密钥。
- 保留域名路由和商家默认站点，不使用 `default_server` 抢占其它业务流量。
- 直接使用 IP 的 HTTPS 会有证书名称不匹配提示，验收优先使用 HTTP IP 入口。

## [1.0.3] - 2026-09-21

- DIY 方案卡透传后端 `copy_text`，提供“复制用料清单”入口。
- 复制入口只在后端提供真实清单时显示，不从前端重新拼接花材，避免口径漂移。

## [1.0.2] - 2026-09-21

- 将此前已上线但未提交的 DIY 卡片修复、商品横向列表、城市过滤依赖固化为提交基线。
- 收录城市下单校验模块和商家服务登录补丁；仅对照远端确认缺失后部署，不重复执行商家补丁。
- 商家补丁不再向终端打印生成的服务密钥。
- UI 截图未通过敏感信息人工复核时保留本地，不纳入发布代码。
- 构建与发布关联 Git commit，生产保留版本、校验值和回退目录。

本项目遵循语义化版本：`MAJOR.MINOR.PATCH`。

- `MAJOR`：不兼容的接口、数据模型或用户流程变更。
- `MINOR`：兼容的新功能。
- `PATCH`：兼容的缺陷修复、安全修复与内部优化。

每次生产发布必须新增版本条目、通过构建与对应回归验证，并记录发布后健康检查结果。

## [1.0.1] - 2026-09-21

### Fixed

- DIY 效果图已回填时，不再因历史 `task_id` 仍存在而持续显示“正在生成”。
- 不再根据现成商品推荐文案伪造“AI 定制花束方案”卡片。
- 移除伪造 DIY 卡触发的模糊生图聊天请求，防止重复推荐。
- 生图轮询结束重置轮询标记；没有图片的超时任务会显示失败状态。

### Changed

- 仅渲染后端结构化返回且标记为 `diy: true` 的 DIY 方案；真实 DIY 方案在同一响应中自动提交效果图任务。

### Verification

- `npm run build`
- 发布前：备份生产 `dist`。
- 发布后：静态服务、本机 HTTPS 与公网 HTTPS 返回 200；`twlh5-api`、`flower-shop` 保持 active。
